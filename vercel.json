{
  "version": 2,
  "buildCommand": "cd client && CI=false npm run build:deploy",
  "outputDirectory": "client/build",
  "installCommand": "cd client && npm install --legacy-peer-deps",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
